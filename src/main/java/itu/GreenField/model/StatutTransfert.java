package itu.GreenField.model;

/**
 * ATTENTION : si cet enum existe déjà dans ton package model
 * (correspondant au type Postgres "statut_transfert"), supprime ce
 * fichier et utilise le tien à la place — ne garde qu'une seule version.
 */
public enum StatutTransfert {
    Cree,
    En_cours,
    Termine
}
